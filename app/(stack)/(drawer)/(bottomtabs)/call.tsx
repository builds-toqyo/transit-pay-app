import { StyleSheet, Text, View } from "react-native";
import React from "react";

const call = () => {
  return (
    <View className=" dark:bg-dark bg-white flex-1">
      <Text className="dark:text-white text-dark">calls</Text>
    </View>
  );
};

export default call;

const styles = StyleSheet.create({});
